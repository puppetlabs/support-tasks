{
  "description": "Execute the PE Support Script and upload it via SFTP",
  "input_method": "environment",
  "supports_noop": false,
  "parameters": {
    "logage": {
      "description": "Log age (in days) to collect. Defaults to '3'",
      "type": "Optional[Pattern[/^[0-9]$/]]"
    },
    "scope": {
      "description": "Scope (comma-delimited) of diagnostics to collect. Defaults to 'base-status,system,puppet-agent,puppetserver,puppetdb,pe'",
      "type": "Optional[Pattern[/^[a-z,]+$/]]"
    },
    "ticket": {
      "description": "Support ticket number. Required",
      "type": "Integer"
    }
  }
}
