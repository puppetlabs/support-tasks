{
  "description": "Backup ssldir and move new CA cert",
  "parameters": {
    "new_cert": {
      "description": "Location of the newly generated CA cert",
      "type": "String"
    }
  },

  "implementations": [
    {"name": "kb0337d_configure_master.sh", "requirements": ["shell"], "files": ["support_tasks/files/common.sh"], "input_method": "environment"}
  ]
}
