{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "This Task will restart the puppet agent process in a randomised period between 0 and the currently set runinterval",
  "parameters": {
  },
  "implementations": [
    {"name": "kb0346a_bash_herd_resolver.sh", "requirements": ["shell"]},
    {"name": "kb0346b_powershell_herd_resolver.ps1", "requirements": ["powershell"]}
  ]
}

